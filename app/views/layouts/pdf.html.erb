<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <title><%= (@order && @order.invoice_number.presence) || 'Invoice' %></title>
  <style>
    /* Basic print-friendly defaults - keep it minimal, invoice template contains main styles */
    body { font-family: Arial, Helvetica, sans-serif; color: #000; margin: 0; padding: 0; }
    .invoice-box { width: 100%; padding: 10px; }
    table { border-collapse: collapse; }
  </style>
</head>
<body>
  <div class="invoice-box">
    <%= yield %>
  </div>
</body>
</html>
