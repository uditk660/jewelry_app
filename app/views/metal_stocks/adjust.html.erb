<h1>Adjust Stock: <%= @metal_stock.store.name %> / <%= @metal_stock.metal.name %></h1>

<p>Current: <strong><%= @metal_stock.available_grams.to_i %></strong> — Price: <%= format_rupees(@metal_stock.price_per_gram) %> / g</p>

<%= form_for @metal_stock, url: do_adjust_metal_stock_path(@metal_stock) do |f| %>
  <div>
    <%= f.label :change_grams, 'Change (grams, use negative to reduce)' %><br>
    <%= f.number_field :change_grams %>
  </div>
  <div>
    <%= f.label :note %><br>
    <%= f.text_area :note %>
  </div>
  <div>
    <%= f.submit 'Apply' %>
  </div>
<% end %>
